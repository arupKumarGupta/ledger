# 🎨 Visual Setup Guide - GitHub Pages + DynamoDB

A visual guide to understand how environment variables flow from GitHub Secrets to your deployed app.

---

## 📊 The Complete Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    GITHUB REPOSITORY                            │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Secrets (Settings → Secrets → Actions)                │   │
│  │                                                          │   │
│  │  1. VITE_AWS_REGION = "ap-south-1"                     │   │
│  │  2. VITE_AWS_ACCESS_KEY_ID = "AKIA..."                │   │
│  │  3. VITE_AWS_SECRET_ACCESS_KEY = "wJal..."            │   │
│  │  4. VITE_DYNAMODB_TABLE_NAME = "expense-manager"       │   │
│  │  5. VITE_DYNAMODB_PARTITION_KEY = "expense-man..."     │   │
│  └────────────────────────────────────────────────────────┘   │
│                             ↓                                   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  GitHub Actions Workflow (.github/workflows/deploy.yml)│   │
│  │                                                          │   │
│  │  - name: Build                                          │   │
│  │    run: yarn build                                      │   │
│  │    env:                                                 │   │
│  │      VITE_AWS_REGION: ${{ secrets.VITE_AWS_REGION }}   │   │
│  │      VITE_AWS_ACCESS_KEY_ID: ${{ secrets... }}         │   │
│  │      ... (all 5 secrets)                                │   │
│  └────────────────────────────────────────────────────────┘   │
│                             ↓                                   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Vite Build Process                                     │   │
│  │                                                          │   │
│  │  • Reads environment variables                          │   │
│  │  • Replaces import.meta.env.VITE_* with actual values  │   │
│  │  • Bundles everything into static files                 │   │
│  │  • Outputs to dist/ directory                           │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    GITHUB PAGES SERVER                          │
│                                                                 │
│  dist/ (Static Files)                                           │
│  ├── index.html                                                 │
│  ├── assets/                                                    │
│  │   ├── index-[hash].js  ← AWS credentials embedded here!   │
│  │   └── index-[hash].css                                      │
│  └── ...                                                        │
└─────────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                       USER'S BROWSER                            │
│                                                                 │
│  Downloads: index.html + index-[hash].js                        │
│  Executes: JavaScript with embedded AWS credentials             │
│                             ↓                                   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  AWS SDK (in browser)                                   │   │
│  │                                                          │   │
│  │  new DynamoDBClient({                                   │   │
│  │    region: "ap-south-1",                                │   │
│  │    credentials: {                                        │   │
│  │      accessKeyId: "AKIA...",                            │   │
│  │      secretAccessKey: "wJal..."                         │   │
│  │    }                                                     │   │
│  │  })                                                      │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                       AWS DYNAMODB                              │
│                                                                 │
│  Table: expense-manager                                         │
│  Region: ap-south-1                                             │
│                                                                 │
│  Item:                                                          │
│  {                                                              │
│    "expense-manager-partition-key-1209": "expense-data",        │
│    "data": { expenseHeads: [...], expenseEntries: [...] },     │
│    "lastModified": "2025-11-12T10:19:00.000Z"                   │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Where Secrets Live

### Local Development
```
Your Computer
├── .env (file)
│   ├── VITE_AWS_REGION=ap-south-1
│   ├── VITE_AWS_ACCESS_KEY_ID=...
│   └── ... (5 variables)
│
└── src/services/dynamodb.ts
    └── import.meta.env.VITE_AWS_REGION ← reads from .env
```

### GitHub Pages Deployment
```
GitHub Repository
├── Secrets (Repository → Settings → Secrets → Actions)
│   ├── VITE_AWS_REGION
│   ├── VITE_AWS_ACCESS_KEY_ID
│   └── ... (5 secrets)
│
└── .github/workflows/deploy.yml
    └── env: ${{ secrets.VITE_AWS_REGION }} ← reads from Secrets
        ↓
    Vite build embeds values
        ↓
    dist/assets/index-[hash].js ← contains actual values
```

---

## 🎯 Step-by-Step Visual

### Step 1: Add GitHub Secrets
```
https://github.com/arupKumarGupta/ledger/settings/secrets/actions

┌────────────────────────────────────────┐
│  Repository secrets                    │
├────────────────────────────────────────┤
│                                        │
│  [New repository secret]               │
│                                        │
│  ✓ VITE_AWS_REGION                     │
│  ✓ VITE_AWS_ACCESS_KEY_ID              │
│  ✓ VITE_AWS_SECRET_ACCESS_KEY          │
│  ✓ VITE_DYNAMODB_TABLE_NAME            │
│  ✓ VITE_DYNAMODB_PARTITION_KEY         │
│                                        │
└────────────────────────────────────────┘
```

### Step 2: Push Code
```
$ git push origin main

┌────────────────────────────────────────┐
│  GitHub Actions                        │
├────────────────────────────────────────┤
│                                        │
│  ⚙️  Deploy to GitHub Pages            │
│      ├── Checkout code                │
│      ├── Setup Node.js                │
│      ├── Install dependencies         │
│      ├── Build (with secrets) ✓       │
│      ├── Upload artifact              │
│      └── Deploy to Pages ✓            │
│                                        │
│  Status: ✅ Success                    │
└────────────────────────────────────────┘
```

### Step 3: Access Deployed App
```
https://arupkumargupta.github.io/ledger/

┌────────────────────────────────────────┐
│  Expense Manager                       │
├────────────────────────────────────────┤
│                                        │
│  [Add Expense Head] [Add Entry]        │
│                                        │
│  Your expenses:                        │
│  • Office Supplies    $1000           │
│  • Travel Costs       $500            │
│                                        │
│  ☁️ Cloud Sync: ✓ Connected           │
│                                        │
└────────────────────────────────────────┘
```

---

## 🔄 Comparison: Local vs GitHub Pages

### Local Development
```
┌─────────────────────────────────────┐
│  YOUR COMPUTER                      │
│                                     │
│  1. Create .env file                │
│     VITE_AWS_REGION=ap-south-1      │
│     VITE_AWS_ACCESS_KEY_ID=...      │
│     ...                             │
│                                     │
│  2. Run: yarn dev                   │
│                                     │
│  3. Vite dev server reads .env      │
│                                     │
│  4. Browser loads from localhost    │
│                                     │
│  5. App connects to DynamoDB        │
└─────────────────────────────────────┘
```

### GitHub Pages
```
┌─────────────────────────────────────┐
│  GITHUB                             │
│                                     │
│  1. Add Secrets (in Settings)       │
│     VITE_AWS_REGION                 │
│     VITE_AWS_ACCESS_KEY_ID          │
│     ...                             │
│                                     │
│  2. Push code to main branch        │
│                                     │
│  3. GitHub Actions builds with      │
│     secrets embedded                │
│                                     │
│  4. Static files deployed to        │
│     GitHub Pages                    │
│                                     │
│  5. Browser loads from GitHub Pages │
│                                     │
│  6. App connects to DynamoDB        │
└─────────────────────────────────────┘
```

---

## 🎨 Data Flow Diagram

```
┌──────────┐
│  USER    │
└────┬─────┘
     │ Adds expense
     ↓
┌──────────────────┐
│  BROWSER         │
│  (React App)     │
└────┬─────────────┘
     │ saveToCloud()
     ↓
┌──────────────────┐
│  dynamodb.ts     │
│  (AWS SDK)       │
└────┬─────────────┘
     │ PutItem
     ↓
┌──────────────────┐
│  DynamoDB Client │
│  (in browser)    │
└────┬─────────────┘
     │ HTTPS request
     ↓
┌──────────────────┐
│  AWS DynamoDB    │
│  (Cloud)         │
└──────────────────┘
     │
     ↓ Data stored
┌──────────────────┐
│  Table:          │
│  expense-manager │
└──────────────────┘
```

---

## 🔒 Security Visualization

### What's Visible
```
GitHub Pages (Public Internet)
├── Anyone can download: index-[hash].js
├── Anyone can view source
└── Anyone can extract: AWS credentials

AWS DynamoDB (Protected by IAM)
├── ❌ Cannot list tables (no permission)
├── ❌ Cannot access other tables (limited scope)
├── ❌ Cannot delete data (if not granted)
└── ✅ Can only read/write expense-manager table
```

### Protection Layers
```
┌────────────────────────────────────────────────────┐
│  Layer 1: IAM User Permissions                     │
│  • Only access to expense-manager table            │
│  • Only PutItem and GetItem operations             │
│  • Cannot delete or modify table structure         │
└────────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────────┐
│  Layer 2: Single Table Access                      │
│  • ARN limits to specific table                    │
│  • Cannot access other AWS services                │
└────────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────────┐
│  Layer 3: Personal Use Only                        │
│  • Only your expense data                          │
│  • No sensitive information stored                 │
│  • Single-user application                         │
└────────────────────────────────────────────────────┘
```

---

## 📊 Timeline of Events

```
T=0: Developer adds GitHub Secrets
     │
     ↓
T=1: Developer pushes code to main
     │
     ↓
T=2: GitHub Actions workflow triggered
     │
     ├─ Checkout code
     ├─ Install dependencies
     ├─ Build with secrets
     └─ Deploy to Pages
     │
     ↓
T=3: Deployment complete ✅
     │
     ↓
T=4: User visits GitHub Pages URL
     │
     ├─ Browser downloads index.html
     ├─ Browser downloads index-[hash].js
     └─ JavaScript executes
     │
     ↓
T=5: User adds expense
     │
     ├─ React component calls saveToCloud()
     ├─ AWS SDK sends request to DynamoDB
     └─ Data saved to cloud ✓
     │
     ↓
T=6: User opens app on another device
     │
     ├─ Browser downloads same files
     ├─ React app calls loadFromCloud()
     └─ Data synced ✓
```

---

## 🎯 The 5 Secrets Explained

```
┌─────────────────────────────────────────────────────────┐
│  Secret #1: VITE_AWS_REGION                             │
│  ────────────────────────────────────────────────────   │
│  Purpose: Tells AWS SDK which region to connect to      │
│  Value:   ap-south-1 (Mumbai, India)                    │
│  Used in: DynamoDBClient({ region: ... })               │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Secret #2: VITE_AWS_ACCESS_KEY_ID                      │
│  ────────────────────────────────────────────────────   │
│  Purpose: Identifies which IAM user is making requests   │
│  Value:   AKIA... (from AWS IAM)                         │
│  Used in: credentials: { accessKeyId: ... }              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Secret #3: VITE_AWS_SECRET_ACCESS_KEY                  │
│  ────────────────────────────────────────────────────   │
│  Purpose: Password for the IAM user (keeps secure)       │
│  Value:   wJal... (from AWS IAM)                         │
│  Used in: credentials: { secretAccessKey: ... }          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Secret #4: VITE_DYNAMODB_TABLE_NAME                    │
│  ────────────────────────────────────────────────────   │
│  Purpose: Which table to read/write data                 │
│  Value:   expense-manager                                │
│  Used in: PutCommand({ TableName: ... })                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Secret #5: VITE_DYNAMODB_PARTITION_KEY                 │
│  ────────────────────────────────────────────────────   │
│  Purpose: Name of the partition key field in table       │
│  Value:   expense-manager-partition-key-1209             │
│  Used in: Key: { [PARTITION_KEY]: 'expense-data' }       │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ Success Indicators

### When It's Working
```
Browser Console:
├── ✅ No "DynamoDB not configured" messages
├── ✅ No AWS error messages
├── ✅ See successful PutItem/GetItem logs
└── ✅ Cloud sync icon shows connected

AWS DynamoDB Console:
├── ✅ New items appear in table
├── ✅ lastModified timestamp updates
└── ✅ Data matches what you entered

GitHub Actions:
├── ✅ Green checkmarks on all steps
├── ✅ "Deploy to GitHub Pages" succeeds
└── ✅ No build errors
```

### When It's Not Working
```
Browser Console:
├── ❌ "DynamoDB not configured"
├── ❌ "Access Denied" errors
├── ❌ "ResourceNotFoundException"
└── ❌ Network errors

GitHub Actions:
├── ❌ Red X on build step
├── ❌ Environment variable not found
└── ❌ Deployment failed

AWS DynamoDB Console:
├── ❌ No new items appearing
└── ❌ Table is empty
```

---

## 🚀 Quick Start Visual

```
START HERE
    │
    ↓
Add 5 GitHub Secrets ──→ [5 min]
    │
    ↓
Push code to main ──→ [1 min]
    │
    ↓
Wait for deployment ──→ [1-2 min]
    │
    ↓
Visit GitHub Pages URL ──→ [instant]
    │
    ↓
Test cloud sync ──→ [instant]
    │
    ↓
DONE! ✅
```

---

## 📚 More Information

- **Detailed Guide**: [GITHUB_SECRETS_SETUP.md](GITHUB_SECRETS_SETUP.md)
- **Quick Checklist**: [GITHUB_PAGES_ENV_CHECKLIST.md](GITHUB_PAGES_ENV_CHECKLIST.md)
- **Complete Setup**: [QUICK_SETUP.md](QUICK_SETUP.md)
- **Documentation Index**: [DOCS_INDEX.md](DOCS_INDEX.md)

---

**Start now**: [GITHUB_PAGES_ENV_CHECKLIST.md](GITHUB_PAGES_ENV_CHECKLIST.md) ⚡

